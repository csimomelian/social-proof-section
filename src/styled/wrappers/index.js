import styled from 'styled-components/macro';

const Wrapper = styled.main`

    flex-grow:1;

`;

const Inner = styled.div`

    margin:0 auto;
    width: 100%;
    max-width: 111.5rem;
`;

export{
    Wrapper,
    Inner
}